# Pinned dependencies for the legacy Python test harness.
# Used by:
#   - tests/test_rules.py
#   - tests/test_logsource.py
#   - tests/regression_tests_runner.py
#
# Pins are exact (==) so CI is reproducible: an unrelated upstream release
# can no longer turn a contributor's PR red. Bump deliberately and let CI
# (which caches on this file's hash) re-validate the new versions.
PyYAML==6.0.3
colorama==0.4.6
