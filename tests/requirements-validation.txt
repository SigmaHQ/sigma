# Pinned dependencies for Sigma rule validation via `sigma check`.
# Used by the `sigma-check` job in .github/workflows/sigma-test.yml.
#
# Pins are exact (==) for reproducible validation. Previously
# pySigma-validators-sigmahq floated on 0.20.* — a patch release of the
# validators (which encode the SigmaHQ conventions) could change the rule
# pass/fail bar for every open PR with no change on our side. Freezing the
# version makes convention bumps an explicit, reviewable commit.
#
# To bump: raise these together, run
#   sigma check --fail-on-error --fail-on-issues \
#     --validation-config tests/sigma_cli_conf.yml rules*
# locally, fix any newly surfaced rule issues, then commit.
pysigma==1.3.3
sigma-cli==3.0.2
pySigma-validators-sigmahq==0.20.2
