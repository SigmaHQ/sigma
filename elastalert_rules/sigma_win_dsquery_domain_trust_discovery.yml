alert:
- debug
description: Detects a discovery of domain trusts
filter:
- query:
    query_string:
      query: (data.win.system.eventID:"4688" AND ((process_path:*\\dsquery.exe AND data.win.eventdata.commandLine:*\-filter* AND data.win.eventdata.commandLine:*trustedDomain*) OR (process_path:*\\nltest.exe AND data.win.eventdata.commandLine:*domain_trusts*)))
index: wazuh-alerts-3.x-*
name: Domain-Trust-Discovery
priority: 3
realert:
  minutes: 0
type: any


